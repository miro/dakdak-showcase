extends layout

block content

  .image-grid__wrapper
    each image in images
      a.image-grid__item(
        href='/image/' + image.storageId
        style={ 'background-image': 'url(' + image.urls.thumb + ')' }
      )


